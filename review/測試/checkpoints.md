# 複習檢查點：測試

## Level 1 記憶檢查

| 檢查項目 | 通過標準 | 對應題目 |
| --- | --- | --- |
| 表單用途與組成 | 不看筆記能說出表單用途、表單域、表單控件、提示信息 | L1-001 |
| `<form>` 常用屬性 | 能說明 `action`、`method`、`name`、`target` | L1-002 |
| 基礎 `<input>` 屬性 | 能分辨 `type`、`name`、`value`、`placeholder`、`maxlength` | L1-003 |
| 選項控件 | 能說出 `radio` 與 `checkbox` 使用時機 | L1-004 |
| 表單按鈕 | 能說出 `submit`、`reset`、`button` 行為與 `<button>` 預設值 | L1-005 |
| 下拉與多行輸入 | 能說出 `<select>`、`<option>`、`<textarea>` 用途與 textarea 預設內容位置 | L1-006 |
| 表單可讀性與禁用狀態 | 能說出 `label`、`fieldset`、`legend`、`disabled` 作用 | L1-007 |
| HTML5 表單能力 | 能列出常見 HTML5 input 類型與 `required`、`autofocus`、`autocomplete` 作用 | L1-008 |

## Level 2 理解檢查

| 檢查項目 | 通過標準 | 對應題目 |
| --- | --- | --- |
| 表單提交範圍 | 能解釋控件放在 `<form>` 外面會造成什麼問題 | L2-001 |
| 資料值與提示文字 | 能說明為什麼 `value` 不能假裝 placeholder，且 password 不等於安全加密 | L2-002 |
| radio 群組 | 能解釋相同 `name` 與 `value` 的角色 | L2-003 |
| 按鈕行為 | 能判斷一般操作按鈕與提交按鈕的差異 | L2-004 |
| 控件選擇 | 能根據固定選項、複選、多行文字選出合適控件 | L2-005 |
| label 與 disabled | 能解釋 for/id 關聯與 disabled 不提交資料的影響 | L2-006 |
| HTML5 type 與體驗屬性 | 能說明特定 type、`required`、placeholder 與 label 的判斷 | L2-007 |

## Level 3 應用檢查

| 檢查項目 | 通過標準 | 對應題目 |
| --- | --- | --- |
| 基本表單結構 | 能建立包含 `action`、`method`、欄位、提示文字與 submit 的表單 | L3-001 |
| hidden 資料 | 能建立同時提交使用者輸入與固定資料的表單 | L3-002 |
| 單選與複選 | 能正確組合 radio 群組與 checkbox 群組 | L3-003 |
| 按鈕除錯 | 能修正未指定 type 造成的意外提交 | L3-004 |
| 分組表單 | 能使用 fieldset、legend、label、select、textarea 組合可讀表單 | L3-005 |
| HTML5 搜尋表單 | 能使用 `type="search"`、`required`、`autofocus`、`autocomplete` 與 label | L3-006 |

## 章節完成標準

- Level 1 題目正確率達到 90% 以上。
- Level 2 題目能不用筆記講出主要原因、差異與判斷依據。
- Level 3 題目至少完成 5 題，且錯題已依 `wrong-question-loop.md` 回流處理。
- 高風險概念清單中沒有尚未處理的連續錯題。
- 能在新需求中先判斷表單範圍、控件類型、提示文字、提交按鈕與必要屬性。
