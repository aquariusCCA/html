> 規定元素內容的文本方向。
> ![dir 屬性值與文字方向對照表](./assets/images/dir-attribute-img-001-5f8241.png)

- 屬性值如下:
    - 在 HTML5 中，dir 屬性可用於任何的 HTML 元素，它會驗證任何HTML元素，但不一定是有用。
    - 在 HTML4 中，dir 元素不能用於：`<base>、<br>、<frame>、<frameset>、<hr>、<iframe>、<param>、<script>`。

```html
<p dir="rtl">文本方向从右到左!</p>
```
