> 標籤語意: 強制換行。

在 HTML 中，一個段落中的文字會從左到右一次排序，直到瀏覽器窗口的右端，然後才自動換行，如果希望某段文本強制換行顯示，就需要使用換行標籤 `<br/>`。

- 場景 : 讓文字強制換行顯示。
- 單詞 break 的縮寫，意為打斷、換行。
- 特點 :
    - `<br/>` 是個單標籤。
    - `<br/>` 標籤只是簡單的開始新的一行，跟段落不一樣，段落之間會插入一些垂直的間距。

```html
第一行文本 <br />第二行文本
```