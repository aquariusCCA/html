# Element / Attribute Index

## HTML 標籤

本篇主要說明 Web 標準、三層分離與 HTML 語意責任，沒有明確教學可作為主條目的 HTML 標籤。筆記中的 `button`、`h1`、`p` 主要用於示範或對照，因此不列入主索引條目。

| 標籤 | 主要用途 | 常見屬性 | 對應段落 |
| --- | --- | --- | --- |

## HTML 屬性

| 屬性 | 常見使用於 | 主要用途 | 對應段落 |
| --- | --- | --- | --- |
| `class` | HTML 元素 | 用 class 名稱連接 HTML 結構與 CSS 樣式，避免把樣式細節直接混入 HTML。 | [把樣式直接塞進 HTML 結構](../../../notes/第01章_寫在前面/06-Web-標準與三層分離.md#把樣式直接塞進-html-結構) |
| `style` | HTML 元素 | 直接在 HTML 上寫樣式；本篇把它作為結構與樣式混雜的反例。 | [把樣式直接塞進 HTML 結構](../../../notes/第01章_寫在前面/06-Web-標準與三層分離.md#把樣式直接塞進-html-結構) |
