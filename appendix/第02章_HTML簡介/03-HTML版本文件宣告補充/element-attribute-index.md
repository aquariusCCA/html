# Element / Attribute Index

## 用途

查找本篇筆記中明確教學的 HTML 標籤與屬性。

## HTML 標籤

本篇筆記主題是 HTML 版本文件宣告。`<!DOCTYPE html>` 在筆記中明確說明不是 HTML 標籤，而是文件類型宣告；範例中的 `<html>`、`<head>`、`<meta>`、`<title>`、`<body>` 只是用來呈現文件骨架，不是本篇主要教學的標籤，因此不建立標籤條目。

| 標籤 | 主要用途 | 常見屬性 | 對應段落 |
| --- | --- | --- | --- |

## HTML 屬性

本篇筆記沒有明確教學特定 HTML 屬性的用法。範例中的 `lang`、`charset`、`http-equiv`、`content`、`xmlns`、`xml:lang` 只作為版本宣告範例的一部分，因此不建立屬性條目。

| 屬性 | 常見使用於 | 主要用途 | 對應段落 |
| --- | --- | --- | --- |
