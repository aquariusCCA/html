# Tag Index

## 用途

用標籤輔助搜尋與交叉分類本篇筆記中的全局屬性、CSS 選取、DOM 資料與選用原則。

## 標籤索引

| Tag | 用途 | 對應段落 |
| --- | --- | --- |
| `#html-global-attributes` | `id`、`class`、`data-*`、`style` 這類可放在多數元素上的全局屬性。 | [問題情境](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#問題情境) |
| `#html-id` | 唯一識別、CSS 精準選取、JavaScript 取得元素與頁面內錨點。 | [`id`：元素的唯一識別](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#id元素的唯一識別) |
| `#html-class` | 元素分類、共用樣式、多元素選取與多個 `class` 寫法。 | [`class`：元素的分類名稱](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#class元素的分類名稱) |
| `#html-data-attributes` | `data-*` 自訂資料、命名規則、`dataset` 與 JavaScript 讀取。 | [`data-*`：元素上的自訂資料](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#data-元素上的自訂資料) |
| `#inline-style` | `style` 行內 CSS、優先權概念與不適合大量重複的原因。 | [`style`：直接寫在元素上的 CSS](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#style直接寫在元素上的-css) |
| `#css-selector` | 透過 `id` 或 `class` 讓 CSS 找到單一或一組元素。 | [`class`：元素的分類名稱](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#class元素的分類名稱) |
| `#javascript-dom` | 用 JavaScript 選取元素、讀取 `dataset` 或設定 `data-*`。 | [`data-*`：元素上的自訂資料](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#data-元素上的自訂資料) |
| `#anchor-link` | 透過 `id` 與 `href="#id"` 建立同頁跳轉。 | [`id`：元素的唯一識別](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#id元素的唯一識別) |
| `#html-best-practices` | 避免重複 `id`、資料塞進 `class`、大量重複行內樣式。 | [常見錯誤](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#常見錯誤) |
