# Scenario Index

## 用途

從實作需求反查本篇筆記中可支援的全局屬性與正確選用方式。

## 實作場景索引

| 我想做什麼 | 需要知道 | 對應段落 |
| --- | --- | --- |
| 替單一元素建立可被 CSS 或 JavaScript 精準找到的識別 | `id`、唯一識別、`getElementById()` | [`id`：元素的唯一識別](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#id元素的唯一識別) |
| 建立頁面內跳轉目標 | `id`、`href="#id"`、頁面內錨點 | [`id`：元素的唯一識別](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#id元素的唯一識別) |
| 讓多個元素共用同一套樣式 | `class`、CSS 類選擇器、重複使用 | [`class`：元素的分類名稱](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#class元素的分類名稱) |
| 讓一個元素同時屬於多個分類 | 多個 `class`、空格分隔 | [`class`：元素的分類名稱](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#class元素的分類名稱) |
| 在 HTML 元素上保存給 JavaScript 讀取的資料 | `data-*`、自訂資料、DOM | [`data-*`：元素上的自訂資料](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#data-元素上的自訂資料) |
| 從 JavaScript 讀取 `data-*` 資料 | `dataset`、`data-animal-type`、`animalType` | [`data-*`：元素上的自訂資料](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#data-元素上的自訂資料) |
| 用 JavaScript 替元素設定自訂資料屬性 | `setAttribute()`、`data-food-type` | [`data-*`：元素上的自訂資料](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#data-元素上的自訂資料) |
| 對單一元素快速加一次性樣式 | `style`、行內 CSS、少量特殊情況 | [`style`：直接寫在元素上的 CSS](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#style直接寫在元素上的-css) |
| 判斷該用 `id`、`class`、`data-*` 還是 `style` | 識別、分類、資料、樣式 | [四個屬性的實務比較](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#四個屬性的實務比較) |
| 修正多個元素共用同一個 `id` 的寫法 | 重複 `id`、改用 `class` | [把多個元素設成同一個 `id`](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#把多個元素設成同一個-id) |
| 修正把商品或狀態資料塞進 `class` 的寫法 | `class`、`data-*`、JavaScript 資料 | [把資料塞進 class 名稱](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#把資料塞進-class-名稱) |
| 避免多個元素重複寫相同的行內樣式 | `style`、`class`、共用 CSS | [大量使用行內樣式](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#大量使用行內樣式) |
