# Topic Index

## 用途

從學習主題快速定位本篇筆記中關於元素識別、分類、自訂資料與行內樣式的段落。

## 主題索引

| 主題 | 對應段落 | 你會學到 |
| --- | --- | --- |
| 全局屬性的角色分工 | [問題情境](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#問題情境) | `id`、`class`、`data-*` 與 `style` 都能補充元素資訊，但用途不同。 |
| 四個屬性的一句話判斷 | [一句話理解](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#一句話理解) | 用「識別、分類、資料、樣式」快速判斷四個屬性的主要功能。 |
| `id` 唯一識別與頁內錨點 | [`id`：元素的唯一識別](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#id元素的唯一識別) | `id` 應唯一，可用於 CSS、JavaScript 與 `href="#id"` 錨點目標。 |
| `class` 分類與共用樣式 | [`class`：元素的分類名稱](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#class元素的分類名稱) | 同一個 `class` 可以重複使用，一個元素也可以有多個 `class`。 |
| `data-*` 自訂資料與 `dataset` | [`data-*`：元素上的自訂資料](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#data-元素上的自訂資料) | 如何在元素上存放自訂資料，並用 JavaScript 透過 `dataset` 讀取。 |
| `style` 行內樣式與維護性 | [`style`：直接寫在元素上的 CSS](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#style直接寫在元素上的-css) | 行內樣式的用途、優先權概念，以及為什麼共用樣式應優先用 `class`。 |
| 常見錯誤與正確選用 | [常見錯誤](../../../notes/第05章_全局屬性/01-元素識別分類與附加資訊屬性.md#常見錯誤) | 避免重複 `id`、把資料塞進 `class`，或大量使用行內樣式。 |
