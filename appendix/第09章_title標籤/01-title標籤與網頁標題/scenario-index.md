# Scenario Index

## 實作場景索引

| 我想做什麼 | 需要知道 | 對應段落 |
| --- | --- | --- |
| 設定瀏覽器分頁上的頁面標題 | 使用 `<title>` 設定 HTML 文件標題 | [title 的主要作用](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#title-的主要作用) |
| 在 HTML 基本結構中加入 `<title>` | `<title>` 要寫在 `<head>` 裡，文字放在開始與結束標籤之間 | [基本語法](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#基本語法) |
| 區分分頁標題和頁面可見主標題 | `<title>` 放在 `<head>`，`<h1>` 放在 `<body>` | [title 和 h1 的差異](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#title-和-h1-的差異) |
| 修正把 `<title>` 寫到 `<body>` 的錯誤 | `<title>` 是文件資訊，應放在 `<head>` 中 | [把 title 寫到 body 裡](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#把-title-寫到-body-裡) |
| 改善太籠統的頁面標題 | 標題要能反映頁面主題，不要只寫「首頁」或「我的網站」 | [標題太籠統](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#標題太籠統) |
| 避免 `<title>` 語法不完整 | `<title>` 需要開始標籤與結束標籤 | [忘記結束標籤](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#忘記結束標籤) |
| 為同一網站的不同頁面寫不同標題 | 每個頁面都應有清楚、具體且能分辨內容的 `<title>` | [實務使用原則](../../../notes/第09章_title標籤/01-title標籤與網頁標題.md#實務使用原則) |
