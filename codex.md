請依 `meta/update-rules.md` 協助判斷這次 HTML 筆記包更新的影響範圍。

1. 章節名稱：第02章_HTML簡介
2. 改動位置：`atomic/第02章_HTML簡介/`
3. 任務 / 改動類型：內容修改 / atomic 內容審查完成
4. 改動摘要：我已經使用 `prompts/_drafts/atomic-content-review-draft.md` 對 `atomic/第02章_HTML簡介/*.md` 完成 Atomic 內容審查。請以目前 `atomic/第02章_HTML簡介/` 中的 atomic notes 作為完成審查後的最新候選 atomic 來源，並檢查它對後續 `notes/` 與章節狀態追蹤的影響。
5. 我希望 AI 判斷：判斷本次 atomic 內容審查完成後的影響範圍、需要檢查的內容、候選重生成或候選同步範圍，以及 `meta/chapter-status.md` 和 `meta/chapter-logs/第02章_HTML簡介.md` 的建議標記。

請先判斷影響範圍，再說明：

1. 最新正確來源目前在哪裡。
2. `origin/第02章_HTML簡介/` 是否需要回查或保持不動。
3. `atomic/第02章_HTML簡介/` 是否可視為已完成 atomic 內容審查。
4. 後續是否可以將 `notes/第02章_HTML簡介/` 列為候選生成範圍。
5. 是否需要把 `notes完成檢查`、`demos`、`practice`、`review`、`appendix` 暫時列為未開始、待確認或候選後續流程。
6. `meta/chapter-status.md` 的建議狀態應如何標記。
7. `meta/chapter-logs/第02章_HTML簡介.md` 應補哪些流程紀錄與下一步。

本次請只做判斷：

- 不改檔。
- 不重生成 notes 或下游材料。
- 不同步 `meta/chapter-status.md`。
- 不修改 `meta/chapter-logs/第02章_HTML簡介.md`。
- 只提出影響範圍、候選處理範圍、狀態建議與建議下一步。
