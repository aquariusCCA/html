請依 `meta/update-rules.md` 協助判斷這次 HTML 筆記包更新的影響範圍。

1. 章節名稱：第02章_HTML簡介
2. 改動位置：`origin/第02章_HTML簡介/*.md`
3. 任務 / 改動類型：資料整理 / alt 與連結文字整理
4. 改動摘要：
   - 已依照 `prompts/_drafts/origin-asset-alt-and-link-text-draft.md` 的完整規則，整理 `origin/第02章_HTML簡介` 下的原始 Markdown 資料。
   - 掃描範圍為 `origin/第02章_HTML簡介/*.md`，共 4 個 Markdown 文件。
   - 實際修改 `03-HTML骨架.md` 與 `04-標籤組成和關係.md` 中的 Markdown 圖片 alt，共 8 筆。
   - 未修改任何資產路徑、實體檔名、hash 或資料夾名稱。
   - 未移動、刪除或新增任何資產檔案。
   - 未發現需要處理的 HTML `<img>`、HTML 本地附件 `<a>` 或 Markdown 本地附件連結。
   - 無需要人工確認項目。
5. 我希望 AI 判斷：
   - 這次 `origin` 層的 alt 與連結文字整理，對 `atomic/`、`notes/`、`appendix/`、`demos/`、`practice/`、`review/` 的影響範圍。
   - 是否需要檢查或候選重生成 `atomic`、`notes` 或下游材料。
   - `meta/chapter-status.md` 中與本章相關欄位的建議標記，特別是 `alt與連結文字` 是否可建議標記為已完成。
   - 是否建議同步或新增 `meta/chapter-logs/第02章_HTML簡介.md` 的流程紀錄。

請先判斷影響範圍，再說明需要檢查的內容、候選重生成或候選同步範圍，以及 `meta/chapter-status.md` 建議標記。

本次請只做影響範圍判斷與狀態建議，不要直接修改檔案、不要重生成內容、不要同步狀態表。
