# 字符集

> 來源：origin/第04章_HTML基本結構標籤/01-HTML基本結構標籤.md / # **3. 字符集**

在 `<meta>` 標籤內，可以通過標籤的 charset 屬性來規定 HTML 文檔應該使用哪種字符編碼。

```html
<head>
  <meta charset="UTF-8">
</head>
```

> charset 常用的值有 : GB2312 、BIG5 、GBK 和 UTF-8，其中 UTF-8 也被稱為萬國碼，基本包含了全世界所有國家需要用到的字符。
