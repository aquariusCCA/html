# hidden 隱藏域

> 來源：origin/第24章_表單標籤/03-表單控件.md / `### 隱藏域`

隱藏域是用戶不可見的一個輸入區域，作用是提交表單的時候攜帶一些固定的數據。

```html
<input type="hidden" name="tag" value="100">
```

<aside>

**常用屬性：**

- `name` 屬性：指定數據的名稱。
- `value` 屬性：指定真正提交的數據。

</aside>
